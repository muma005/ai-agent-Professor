[pytest]
testpaths = tests
markers =
    slow: marks tests as slow (> 30 seconds)
    regression: marks tests requiring API keys and real data
    fault_injection: marks fault injection tests
    smoke: marks quick smoke tests

# Default: skip slow and regression tests
addopts = -m "not slow and not regression"

# Timeout per test
timeout = 60
timeout_method = thread

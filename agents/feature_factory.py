# agents/feature_factory.py
# -------------------------------------------------------------------------
# Phase 3 stub — pass-through until real feature engineering is built.
# Registered in the graph so supervisor_replan can route here.
# -------------------------------------------------------------------------

from core.state import ProfessorState


def run_feature_factory(state: ProfessorState) -> ProfessorState:
    """Pass-through stub. Replaced with real implementation in Phase 3."""
    print("[FeatureFactory] Stub — passing through (Phase 3 implementation pending)")
    return state

[pytest]
markers =
    integration: marks tests as integration tests that require external APIs or actual model training (deselect with '-m "not integration"')
    slow: marks tests as slow (deselect with '-m "not slow"')
    
addopts = --strict-markers
testpaths = tests
